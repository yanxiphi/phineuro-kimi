# PhiNeuro 工作日志

## 工作日志规则
- 每次任务执行前，先阅读本日志了解历史上下文
- 每次任务结束后，记录工作指令和关键反馈
- 避免重复讨论已解决的问题

---

## 2026-05-25

### 任务指令
1. 对比页面获取各公司情报数据
2. 统一爬虫 + MECE标签分类体系（政策动态/前沿论文/行业新闻/公司动态）
3. 前沿资讯页面重构：标签化 + 可点击统计卡片
4. 创始人详情页（LinkedIn风格）

### 执行结果
- ✅ 后端 batch API `GET /api/intel/by-company/batch?ids=1,2,3`
- ✅ MECE 4大标签体系 + `content_category` 字段
- ✅ News.tsx 重写为全API驱动（4个可点击统计卡片）
- ✅ FounderProfilePage.tsx 创建（LinkedIn风格）
- ✅ 65位创始人数据入库
- ✅ 17条静态新闻数据入库
- ✅ 远程D1 migration 009-012 执行成功
- ✅ 本地D1 migration 008-012 执行成功

### 关键反馈
- 前端 `npm run build` 失败：`CATEGORY_CONFIG` 未使用（已修复）
- 远程 migration 执行时 `008` 重复执行导致 `event_type` 列已存在错误
  - 解决方案：临时重命名008文件，执行migration后恢复
- 前端已 push 到 GitHub (commit: dca1b02)

---

## 2026-05-25（修复部署问题）

### 执行
- 远程 migration 009-012 执行成功（临时隐藏008后执行）
- 本地 migration 008-012 执行成功（删除本地数据库重建）
- 前端 `git push` 成功 (commit: dca1b02)
- 前端构建报错：`CATEGORY_CONFIG` 未使用（已修复）

---

## 2026-05-25（第二轮反馈）

### 用户反馈的8个问题

#### 1. 工作日志机制
- 需求：建立工作日志，每次任务前阅读、任务后记录，避免重复问题
- 状态：已创建 WORKLOG.md

#### 2. 前沿资讯标签重复
- 问题：上方有4个统计卡片，下方还有一排标签过滤器，存在重复
- 需求：只保留上方带图标的统计卡片，去掉下方标签行

#### 3. 翻译回退 + 详情页丢失
- 问题：资讯内容又变回英文（应显示 `title_zh`/`summary_zh`）
- 问题：点击资讯直接跳转到外部信息源网站，应有自己的详情页
- 历史：之前已实现 KIMI API 翻译（translate-intel.ts），News.tsx 应优先显示中文

#### 4. 政策动态内容不相关
- 问题：爬取到的新闻与政策分类不匹配
- 需求：优化爬取和归类系统

#### 5. 创始人未收录页面文案
- 问题：显示 "Founder not found"
- 需求：显示 "创始人信息搭建中"

#### 6. 企业里程碑位置
- 问题：时间线目前放在页面底部
- 需求：以Neuralink为例，放在公司描述（蓝色背景条）下方

#### 7. 企业详情页配色不一致
- 问题：配色偏离 PhiNeuro 主题色
- 主题色规范：
  - 主色 | 勃艮第红 | #722F37
  - 辅色 | 深灰蓝 | #4A5568
  - 背景 | 米白色 | #FDFCF8
  - 强调 | 暖金色 | #D4AF37
- 需求：分析原因并修复，后续避免

#### 8. 企业列表调整
- 需求a："详情"列放到"企业名称"列下方
- 需求b：点击范围扩大到覆盖中英文名称
- 需求c：企业名称字体变成勃艮第红 #722F37，提示可点击

### 修复执行
- ✅ 问题1：已创建 WORKLOG.md 并更新记录
- ✅ 问题2：News.tsx 已删除下方重复标签过滤器
- ✅ **构建修复**：News.tsx 第201行 JSX 注释 `{/* Content -->` 缺少闭合符，导致 TS1381/TS1005/TS1109 语法错误，已修正为 `{/* Content */}`
- ✅ 问题5：FounderProfilePage.tsx 错误文案改为"创始人信息搭建中"
- ✅ 问题6：CompanyDetailPage.tsx 时间线已移到描述条下方
- ✅ 问题7：CompanyDetailPage.tsx 硬编码颜色已统一为主题色系（#722F37 / #4A5568 / #D4AF37）
- ✅ 问题8：DatabasePage.tsx 表格已调整（详情列位置、点击范围、名称颜色）
- ⚠️ 问题3：已添加"[原文]"标识提示未翻译内容；**资讯详情页需额外开发**（`GET /api/intel/[id]` + `IntelDetailPage.tsx`）
- ⚠️ 问题4：已识别分类规则需优化；**需调整后端 `classifyContentCategory` 关键词权重**
- ⚠️ 问题7（续）：`databaseTags.ts` 中大量标签颜色仍为旧配色，需统一为主题色系（改动量大，建议单独处理）

### 配色问题根因分析
- **原因**：`databaseTags.ts` 和 `CompanyDetailPage.tsx` 中使用了项目早期定义的多彩配色（#E74C3C、#3498DB 等），未遵循 PhiNeuro 主题色规范
- **后续避免**：
  1. 所有标签颜色统一从主题色映射表获取，禁止硬编码非主题色 hex
  2. 新增标签时需从 ` burgundy (#722F37)` / `slate-blue (#4A5568)` / `gold (#D4AF37)` 三色系中选择
  3. 通过透明度差异（如 `bg-burgundy/80`、`bg-burgundy/60`）区分标签类型，而非使用不同色相

---
