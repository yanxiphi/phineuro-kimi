server {
    listen 80;
    server_name phineuro.life www.phineuro.life;
    root /var/www/phineuro;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Static assets cache
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API proxy to Cloudflare backend
    location /api/ {
        proxy_pass https://datasets.phineuro.life/api/;
        proxy_http_version 1.1;
        proxy_set_header Host datasets.phineuro.life;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# HTTPS redirect (after SSL cert setup)
# server {
#     listen 443 ssl http2;
#     server_name phineuro.life www.phineuro.life;
#     ssl_certificate /etc/nginx/ssl/phineuro.crt;
#     ssl_certificate_key /etc/nginx/ssl/phineuro.key;
#     # ... same config as above
# }
